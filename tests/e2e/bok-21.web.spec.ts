import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Anonymous user searches for a valid topic and views the article successfully (happy path)", async ({ page }) => {
    // step: Navigate to https://www.wikipedia.org
    // step: Select or confirm the English language edition to reach the Wikipedia homepage
    // step: Verify the homepage loads without prompting for login or registration
    // step: Click into the search bar on the homepage
    // step: Type the topic 'Machine learning'
    // step: Press Enter or click the search/magnifier button
    // step: Observe the resulting page
    // step: Scroll through the article from the lead section to the bottom
    // expect: Wikipedia homepage is accessible to an anonymous user with no login required
    // expect: The search bar accepts the typed text and may display auto-suggestions
    // expect: After submitting, the relevant article page 'Machine learning' loads successfully with HTTP success (no error page)
    // expect: The article title, lead paragraph, table of contents, section headings and body text are displayed
    // expect: Article content is fully readable (legible font, no truncated or overlapping text) and no paywall, captcha or access restriction is shown
    // expect: Images/links within the article render and the page can be scrolled to the end without errors
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Submitting an empty search bar returns a blank page with no search results", async ({ page }) => {
    // step: Click into the search bar without typing any characters
    // step: Confirm the search field is empty (no spaces)
    // step: Press Enter or click the search/magnifier button
    // step: Observe the resulting page
    // expect: A blank search page is displayed with no search results listed
    // expect: No article page is opened and no unexpected system/500 error is shown
    // expect: The search bar remains available for a new query
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Searching an invalid/nonsense term shows 'There were no results matching the query'", async ({ page }) => {
    // step: Click into the search bar
    // step: Type the invalid term 'zxqwvktpplmndfgh123'
    // step: Press Enter or click the search/magnifier button
    // step: Observe the search results page
    // expect: The search results page loads without crashing
    // expect: The message 'There were no results matching the query' is displayed
    // expect: No article content is rendered and no matching result links are listed
    // expect: The searched term is retained in the search field so the user can correct it
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Search string longer than 500 characters returns maximum length error message", async ({ page }) => {
    // step: Click into the search bar
    // step: Paste the prepared string of more than 500 characters
    // step: Verify the pasted string length is greater than 500 characters
    // step: Press Enter or click the search/magnifier button
    // step: Observe the resulting page
    // expect: The search request is submitted and an error page/message is displayed
    // expect: The error message reads 'An error has occurred while searching: Search request is longer than the maximum allowed length'
    // expect: No article content or result list is displayed
    // expect: The application does not hang, crash or show an unhandled exception/stack trace
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Boundary: search string of exactly 500 characters is accepted and 501 characters is rejected", async ({ page }) => {
    // step: Click into the search bar and paste the 500-character string
    // step: Verify the character count in the field is exactly 500
    // step: Press Enter to submit the search
    // step: Record the page response
    // step: Clear the search bar and paste the 501-character string
    // step: Verify the character count in the field is exactly 501
    // step: Press Enter to submit the search
    // step: Record the page response
    // expect: For the 500-character string the search is processed normally and a results page is shown (typically 'There were no results matching the query'), with no maximum-length error
    // expect: For the 501-character string the error 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed
    // expect: The 500/501 character limit boundary behaves consistently on repeated attempts
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Search containing only whitespace and special characters does not return an article", async ({ page }) => {
    // step: Click into the search bar
    // step: Type three space characters followed by the string '@@@###$$$'
    // step: Press Enter or click the search/magnifier button
    // step: Observe the resulting page
    // expect: No article page is opened
    // expect: A no-results page is displayed with the message 'There were no results matching the query' (or a blank result page if the input is treated as empty)
    // expect: No script error, HTTP 500 or unhandled exception is shown and the page layout remains intact
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
