import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Search for a valid topic from the Wikipedia homepage and view the article", async ({ page }) => {
    // step: Open the browser and navigate to https://www.wikipedia.org
    // step: Select English as the language (or open https://en.wikipedia.org)
    // step: Click the search bar on the homepage
    // step: Type the topic 'Solar System'
    // step: Press Enter or click the search (magnifier) button
    // step: If a results list is displayed, click the article titled 'Solar System'
    // step: Scroll through the article from top to bottom
    // expect: The Wikipedia homepage loads without prompting for login or registration
    // expect: The search bar accepts the typed text and may show autocomplete suggestions
    // expect: The 'Solar System' article page loads successfully with HTTP 200 and no error message
    // expect: The page title matches the searched topic
    // expect: Article content (lead paragraph, infobox, sections, images, references) is fully visible and readable
    // expect: The full article is accessible without any paywall, login, or access restriction
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Verify Wikipedia article is accessible to an anonymous user without login", async ({ page }) => {
    // step: Open a private/incognito browser window
    // step: Navigate to https://en.wikipedia.org
    // step: Confirm that 'Log in' and 'Create account' links are shown (i.e., no active session)
    // step: Search for 'Photosynthesis' using the search bar and submit
    // step: Read the opening section of the returned article
    // step: Refresh the article page
    // expect: The homepage and article page load without redirecting to a login page
    // expect: No login, sign-up, or subscription prompt blocks the content
    // expect: The 'Photosynthesis' article content is fully readable for the anonymous user
    // expect: After refresh, the article still loads successfully without authentication
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Submit an empty search field and verify a blank page with no search results", async ({ page }) => {
    // step: Click inside the search bar without typing any text
    // step: Ensure the search bar is completely empty (no spaces)
    // step: Press Enter or click the search button
    // step: Observe the resulting page
    // expect: The search is submitted without a browser or server crash
    // expect: A blank search page is displayed with no search results listed
    // expect: No article is opened and no results count is shown
    // expect: No unexpected exception or stack trace is displayed
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Search an invalid / non-existent term and verify the no-results error message", async ({ page }) => {
    // step: Click the search bar
    // step: Type the invalid string 'qwzxjvplkmghtrbn123'
    // step: Press Enter or click the search button
    // step: Read the message displayed on the search results page
    // expect: The search results page loads successfully
    // expect: No article page is opened
    // expect: The message 'There were no results matching the query' is displayed
    // expect: The search bar retains the entered query so it can be corrected
    // expect: No application error, blank screen, or crash occurs
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Search with text longer than 500 characters and verify the maximum length error", async ({ page }) => {
    // step: Click the search bar
    // step: Paste the prepared 600-character string into the search bar
    // step: Verify the pasted text length is greater than 500 characters
    // step: Press Enter or click the search button
    // step: Read the message displayed on the resulting page
    // expect: The search request is submitted and the page responds without hanging
    // expect: The error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed
    // expect: No search results and no article content are shown
    // expect: The page remains usable and a new shorter search can be performed
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Boundary check on search query length at 500 and 501 characters", async ({ page }) => {
    // step: Click the search bar and paste the string of exactly 500 characters
    // step: Verify the character count is exactly 500
    // step: Press Enter to submit the search
    // step: Record the message or results shown
    // step: Clear the search bar and paste the string of exactly 501 characters
    // step: Verify the character count is exactly 501
    // step: Press Enter to submit the search
    // step: Record the message or results shown
    // expect: For the 500-character query, the maximum-length error is NOT shown; the search executes and returns either results or 'There were no results matching the query'
    // expect: For the 501-character query, the error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed
    // expect: No results are returned for the 501-character query
    // expect: No crash, timeout, or unhandled exception occurs in either case
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
