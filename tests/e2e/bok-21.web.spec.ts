import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Anonymous user can search a topic and view the Wikipedia article (happy path)", async ({ page }) => {
    // step: Open the browser in a private/incognito window.
    // step: Navigate to https://www.wikipedia.org.
    // step: Confirm the homepage loads without asking for login or registration.
    // step: Click the search bar on the homepage.
    // step: Type the topic "Artificial intelligence".
    // step: Press Enter (or click the search/magnifier button).
    // step: Observe the page that loads.
    // step: Scroll through the article from the top to the bottom.
    // expect: Wikipedia homepage is accessible to an anonymous user with no login prompt or paywall.
    // expect: The search bar accepts the typed text and shows suggestion dropdown entries.
    // expect: After submitting, the article page for "Artificial intelligence" loads successfully with HTTP 200 and no error message.
    // expect: The article title, lead summary paragraph, table of contents, section headings and body text are displayed.
    // expect: Article text is legible (no overlapping/cut-off text) and images/tables render correctly.
    // expect: The page remains fully readable while scrolling and no login is required at any point.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Search with empty input returns a blank search result page", async ({ page }) => {
    // step: Click inside the search bar without typing any text.
    // step: Verify the search bar is empty (clear any pre-filled text).
    // step: Press Enter to submit the empty search.
    // step: Observe the resulting page.
    // expect: The search is submitted without the application crashing.
    // expect: A blank search page is displayed with no search results listed.
    // expect: No article page is opened and no unhandled/technical error is shown to the user.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Search with invalid/nonsense text shows 'no results matching the query' message", async ({ page }) => {
    // step: Click the search bar on the homepage.
    // step: Type the invalid text "qwzxvkjhgfdplmnbzz123".
    // step: Press Enter to submit the search.
    // step: Read the message displayed on the search results page.
    // expect: The search results page loads successfully.
    // expect: The message "There were no results matching the query" is displayed.
    // expect: No article content is shown and no results list is returned.
    // expect: The search bar remains available so the user can retry a new search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Search with text longer than the maximum allowed length shows length error message", async ({ page }) => {
    // step: Click the search bar on the homepage.
    // step: Paste the prepared 600-character string into the search bar.
    // step: Press Enter to submit the search.
    // step: Read the message displayed on the resulting page.
    // expect: The search is submitted and the results page loads without the browser hanging or crashing.
    // expect: The error message "An error has occurred while searching: Search request is longer than the maximum allowed length" is displayed.
    // expect: No article content and no search results are returned.
    // expect: The page layout is not broken by the long input string.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Boundary check: search query of exactly 500 characters vs 501 characters", async ({ page }) => {
    // step: Click the search bar on the homepage.
    // step: Paste the 500-character string into the search bar and verify the character count is exactly 500.
    // step: Press Enter to submit the search and observe the result.
    // step: Return to the homepage and clear the search bar.
    // step: Paste the 501-character string into the search bar and verify the character count is exactly 501.
    // step: Press Enter to submit the search and observe the result.
    // expect: For the 500-character query, the search is processed and the length error is NOT shown; the page shows either search results or "There were no results matching the query".
    // expect: For the 501-character query, the error message "An error has occurred while searching: Search request is longer than the maximum allowed length" is displayed.
    // expect: In both cases the application handles the input gracefully with no crash, timeout, or broken layout.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Search with special characters and leading/trailing spaces only", async ({ page }) => {
    // step: Click the search bar on the homepage.
    // step: Type only spaces (press the space bar 5 times) and press Enter.
    // step: Observe the resulting page.
    // step: Return to the homepage and clear the search bar.
    // step: Type the special characters "@@@###$$$%%%" and press Enter.
    // step: Observe the resulting page and the message displayed.
    // expect: The spaces-only query is treated as an empty search: a blank search page with no results is displayed and no article is opened.
    // expect: The special-characters query returns the message "There were no results matching the query".
    // expect: No unhandled exception, stack trace, or raw server error is displayed to the anonymous user in either case.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
