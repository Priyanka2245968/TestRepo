import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Happy path - Anonymous user searches for a valid topic and views the Wikipedia article", async ({ page }) => {
    // step: Open the browser in private/incognito mode.
    // step: Navigate to https://www.wikipedia.org.
    // step: Verify the homepage loads without prompting for login or registration.
    // step: Click into the search bar on the homepage.
    // step: Type the topic 'Artificial intelligence' into the search bar.
    // step: Press Enter (or click the search/magnifier button) to submit the search.
    // step: Observe the page that loads.
    // step: Scroll through the article from top to bottom.
    // expect: Wikipedia homepage is accessible to an anonymous user with no login or access restriction.
    // expect: The search bar is visible and accepts text input.
    // expect: After submitting, the relevant article page 'Artificial intelligence' loads successfully with HTTP 200 and no error message.
    // expect: The article title matches the searched topic and the URL points to the corresponding article (e.g., /wiki/Artificial_intelligence).
    // expect: Article content (introduction, sections, headings, images, references) is fully rendered, legible and not truncated or overlapping.
    // expect: The full article can be read and scrolled without any login, paywall or subscription prompt.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Happy path - Topic is discovered using search bar autocomplete suggestions", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Type the partial text 'Solar sys' into the search bar.
    // step: Wait for the suggestion dropdown to appear.
    // step: Select the suggestion 'Solar System' from the dropdown list.
    // step: Observe the loaded page.
    // expect: A dropdown list of matching topic suggestions is displayed while typing.
    // expect: The suggestion 'Solar System' is present in the list.
    // expect: Selecting the suggestion navigates directly to the 'Solar System' article page.
    // expect: The article page loads successfully and its content is clearly visible and readable.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Negative - Submitting an empty search bar returns a blank search results page", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Do not enter any text (leave the field empty).
    // step: Press Enter (or click the search button) to submit the search.
    // step: Observe the resulting page.
    // expect: The search is submitted and a blank search page is displayed with no search results listed.
    // expect: No article is opened and no result entries are shown.
    // expect: No unexpected error, exception trace or broken layout is displayed.
    // expect: The search bar remains available so the user can enter a new query.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Negative - Invalid / non-existent search text shows 'no results matching the query' message", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Enter the invalid text 'qwrtzxcvbnmlkjhgfdsa12345'.
    // step: Press Enter to submit the search.
    // step: Observe the resulting page and message.
    // expect: The search results page loads successfully.
    // expect: The message 'There were no results matching the query' is displayed.
    // expect: No article content is displayed and no result list items are shown.
    // expect: The application does not crash and the user can perform a new search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Negative - Search text longer than 500 characters shows maximum length error message", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Paste the prepared string of 501+ characters (e.g., 600 characters) into the search bar.
    // step: Press Enter to submit the search.
    // step: Observe the resulting page and message text.
    // expect: The search results page loads without the browser hanging or crashing.
    // expect: The error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: No article content and no search results are returned.
    // expect: The message text matches exactly (wording, punctuation) as specified in the acceptance criteria.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Boundary - Search text of exactly 500 characters and exactly 501 characters", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Paste the string of exactly 500 characters into the search bar and verify the character count is 500.
    // step: Press Enter to submit the search.
    // step: Record the page response and any message displayed.
    // step: Clear the search bar.
    // step: Paste the string of exactly 501 characters and verify the character count is 501.
    // step: Press Enter to submit the search.
    // step: Record the page response and any message displayed.
    // expect: For the 500-character query the length limit error is NOT shown; the search is processed and a normal search results page is returned (e.g., 'There were no results matching the query' if nothing matches).
    // expect: For the 501-character query the message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: No results are returned for the 501-character query.
    // expect: In both cases the page layout remains intact and the application does not crash.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-07 Negative - Search bar input containing only whitespace characters", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Press the space bar five times so the field contains only whitespace.
    // step: Press Enter to submit the search.
    // step: Observe the resulting page.
    // expect: The whitespace-only input is treated as an empty search.
    // expect: A blank search page with no search results is displayed.
    // expect: No article is opened and no error trace or broken layout appears.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
