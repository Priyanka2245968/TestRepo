import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Anonymous user searches a valid topic and views the Wikipedia article successfully", async ({ page }) => {
    // step: Open the browser in a normal (non-logged-in) session.
    // step: Navigate to https://www.wikipedia.org.
    // step: Confirm the homepage loads without prompting for login or registration.
    // step: Click into the search bar on the homepage.
    // step: Type the topic 'Artificial intelligence'.
    // step: Press Enter or click the search/magnifier button.
    // step: Wait for the result page to load.
    // step: Scroll through the article from top to bottom.
    // expect: Wikipedia homepage is accessible to an anonymous user with no login barrier.
    // expect: The search bar accepts the typed text and shows suggestion dropdown entries.
    // expect: The 'Artificial intelligence' article page loads successfully with HTTP 200 and no error banner.
    // expect: The article title matches the searched topic.
    // expect: Article content (introduction, sections, table of contents, images, references) is displayed, readable, and not truncated or hidden behind a paywall/login wall.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Submitting an empty search bar returns a blank page with no search results", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Do not type any characters (leave the field empty).
    // step: Press Enter or click the search button.
    // step: Observe the resulting page.
    // expect: No article page is opened.
    // expect: A blank search page is displayed with no search results listed.
    // expect: No system exception, stack trace, or broken page is shown.
    // expect: The search bar remains available for a new search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Searching an invalid/nonexistent topic shows 'There were no results matching the query'", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Type the invalid text 'qwrtzxplmvbnhjklzz1234'.
    // step: Press Enter or click the search button.
    // step: Wait for the search results page to load.
    // step: Read the message displayed on the page.
    // expect: No article page is opened.
    // expect: The search results page displays the message: 'There were no results matching the query'.
    // expect: The searched text remains visible in the search field.
    // expect: The page renders normally with no application error or crash.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Searching text longer than 500 characters shows maximum length error message", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Paste the prepared string of 501+ characters into the search bar.
    // step: Press Enter or click the search button.
    // step: Wait for the response page to load.
    // step: Read the message displayed on the page.
    // expect: No article page is opened.
    // expect: The error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: No results list is shown.
    // expect: The page remains usable and a new search can be performed.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Boundary: search text of exactly 500 characters is accepted without length error", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Paste the prepared 500-character string into the search bar.
    // step: Verify the field contains exactly 500 characters (count using a character counter tool if needed).
    // step: Press Enter or click the search button.
    // step: Wait for the search results page to load.
    // step: Read the message or results displayed.
    // expect: The search request is processed without the message 'An error has occurred while searching: Search request is longer than the maximum allowed length'.
    // expect: Either matching search results are listed or the message 'There were no results matching the query' is shown, depending on the string content.
    // expect: The page renders correctly with no application error or crash.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Special characters only in search bar are handled gracefully", async ({ page }) => {
    // step: Click into the search bar on the Wikipedia homepage.
    // step: Type the text '@@@###$$$%%%'.
    // step: Press Enter or click the search button.
    // step: Wait for the response page to load.
    // step: Observe the displayed message and page state.
    // expect: No article page is opened.
    // expect: The message 'There were no results matching the query' is displayed (or an empty result list with no results).
    // expect: No script error, injection behaviour, or unhandled exception occurs.
    // expect: The search bar remains functional for a subsequent valid search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
