import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Search for a valid topic from the Wikipedia homepage and view the article (happy path)", async ({ page }) => {
    // step: Open the browser and navigate to https://www.wikipedia.org
    // step: Select English as the language / click the 'English' link to reach the English Wikipedia homepage
    // step: Click into the search bar at the top of the page
    // step: Type the topic 'Artificial intelligence'
    // step: Press Enter (or click the search/magnifier button)
    // step: Read the loaded page and scroll through the article body
    // expect: The Wikipedia homepage loads without prompting for login or registration
    // expect: The search bar accepts the typed text and shows suggestion dropdown
    // expect: After submitting, the 'Artificial intelligence' article page loads successfully with HTTP 200 and no error message
    // expect: The article title matches the searched topic and the URL points to /wiki/Artificial_intelligence
    // expect: Article content (lead paragraph, sections, table of contents, images) is fully visible and readable without any paywall, login wall or access restriction
    // expect: Page scrolls smoothly and text is legible at default zoom
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Verify Wikipedia article is accessible to an anonymous user without login", async ({ page }) => {
    // step: Navigate to https://en.wikipedia.org
    // step: Confirm no user name is shown in the top-right personal toolbar
    // step: Search for 'Solar System' using the search bar and press Enter
    // step: Scroll to the bottom of the loaded article
    // step: Click any internal link inside the article (e.g. 'Sun')
    // expect: Homepage displays 'Log in' / 'Create account' links, confirming anonymous session
    // expect: The 'Solar System' article opens without any login prompt or CAPTCHA
    // expect: Full article content, references and footer are reachable while anonymous
    // expect: The linked article ('Sun') also opens successfully without authentication
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Submit search with empty search bar - no results are displayed", async ({ page }) => {
    // step: Click into the search bar without typing any characters
    // step: Verify the search bar is empty
    // step: Press Enter (or click the search button)
    // step: Observe the resulting page
    // expect: The search is submitted with an empty query
    // expect: A blank search results page is displayed with no search result entries listed
    // expect: No article page is opened and no unhandled exception, stack trace or broken page is shown
    // expect: The search bar remains available for a new search
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Search with invalid / non-existent text returns 'no results matching the query' message", async ({ page }) => {
    // step: Click into the search bar
    // step: Type the invalid string 'qwrtzxcvbnmlkjhgfdsa12345zzz'
    // step: Press Enter (or click the search button)
    // step: Read the message displayed on the search results page
    // expect: No suggestion is offered for the invalid string
    // expect: The search results page loads successfully
    // expect: The message 'There were no results matching the query' is displayed
    // expect: No article content is rendered and the page does not crash
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Search with text longer than 500 characters returns maximum length error", async ({ page }) => {
    // step: Click into the search bar
    // step: Paste the 600-character test string into the search bar
    // step: Press Enter (or click the search button)
    // step: Read the message displayed on the resulting page
    // expect: The full 600-character string is accepted into the search field
    // expect: The search results page loads without crashing
    // expect: The error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed
    // expect: No article or search result list is displayed
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Boundary check on search query length - 500 characters vs 501 characters", async ({ page }) => {
    // step: Click into the search bar and paste test string A (exactly 500 characters)
    // step: Press Enter and observe the resulting page and any message
    // step: Return to the Wikipedia homepage
    // step: Click into the search bar and paste test string B (exactly 501 characters)
    // step: Press Enter and observe the resulting page and any message
    // expect: For the 500-character query the length error is NOT shown; the search executes and returns either results or the message 'There were no results matching the query'
    // expect: For the 501-character query the error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed
    // expect: In both cases the page renders correctly with no truncated layout, crash or unhandled error
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
