import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Anonymous user searches for a valid topic and views the article successfully (happy path)", async ({ page }) => {
    // step: Open the browser in private/incognito mode.
    // step: Navigate to https://www.wikipedia.org.
    // step: Select the English language option (or confirm English is selected in the search language dropdown).
    // step: Click the search bar on the homepage.
    // step: Type the topic name: Photosynthesis
    // step: Press Enter (or click the search/magnifier button).
    // step: Observe the loaded page.
    // step: Scroll through the article from top to bottom.
    // expect: The Wikipedia homepage loads without prompting for login or registration.
    // expect: The search bar accepts the typed text and may show search suggestions.
    // expect: After submitting, the article page for 'Photosynthesis' loads successfully with HTTP 200 and no error banner.
    // expect: The page title/first heading matches the searched topic 'Photosynthesis'.
    // expect: Article content (lead paragraph, sections, infobox, images, references) is fully visible and readable without truncation or overlap.
    // expect: The whole article is reachable by scrolling and no login wall, paywall, or access restriction appears at any point.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Verify article access without authentication (no login barrier)", async ({ page }) => {
    // step: Clear browser cookies and cache.
    // step: Navigate directly to https://en.wikipedia.org/wiki/Solar_System.
    // step: Observe the page header and content area.
    // step: Click any internal link inside the article (e.g. 'Jupiter').
    // step: Observe the newly opened page.
    // expect: The article page loads successfully for the anonymous user.
    // expect: No login, sign-up, or subscription prompt blocks reading the content.
    // expect: The header shows anonymous-user options (e.g. 'Create account' / 'Log in' links) but content stays accessible.
    // expect: The internal link opens the linked article successfully, still without authentication.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Submit empty search from the Wikipedia homepage search bar", async ({ page }) => {
    // step: Click the search bar on the homepage without typing any text.
    // step: Verify the search bar is empty (no spaces, no characters).
    // step: Press Enter (or click the search button).
    // step: Observe the resulting page.
    // expect: The search is submitted without a query term.
    // expect: A blank search page is displayed with no search results listed.
    // expect: No article page is opened and no unexpected system/stack-trace error is shown.
    // expect: The application remains stable and the user can enter a new search term.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Search with an invalid / non-existent text returns 'no results' message", async ({ page }) => {
    // step: Click the search bar.
    // step: Type the invalid random string: qwzxklmvpotrbnjhgf12345
    // step: Press Enter to submit the search.
    // step: Observe the search results page message.
    // expect: The search results page loads successfully.
    // expect: The message 'There were no results matching the query' is displayed.
    // expect: No article is opened and no result entries are listed.
    // expect: The search bar retains the entered term and remains editable for a new search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Search with text longer than the maximum allowed length shows length error", async ({ page }) => {
    // step: Click the search bar.
    // step: Paste the prepared 600-character string into the search bar.
    // step: Press Enter to submit the search.
    // step: Observe the message displayed on the resulting page.
    // expect: The search request is submitted and the page loads without crashing.
    // expect: The error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: No search results and no article content are shown.
    // expect: The user can clear the field and perform a new valid search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Boundary check on search query length: exactly 500 characters vs 501 characters", async ({ page }) => {
    // step: Click the search bar and paste String A (exactly 500 characters).
    // step: Verify the character count of the entered text is 500.
    // step: Press Enter to submit the search and observe the result.
    // step: Clear the search bar completely.
    // step: Paste String B (exactly 501 characters) into the search bar.
    // step: Verify the character count of the entered text is 501.
    // step: Press Enter to submit the search and observe the result.
    // expect: For the 500-character query, the search is processed and the length error message is NOT displayed; the page shows a normal search results page (typically 'There were no results matching the query').
    // expect: For the 501-character query, the error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: In both cases the page loads without crashing, freezing, or displaying a raw system error.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
