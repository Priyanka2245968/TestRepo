import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Anonymous user can access the Wikipedia homepage without login", async ({ page }) => {
    // step: Open the browser in a new private/incognito window.
    // step: Navigate to https://www.wikipedia.org.
    // step: Observe the page that loads.
    // step: Verify no login or registration prompt blocks access to the page.
    // step: Select the English language link (or confirm the language selector is available).
    // expect: The Wikipedia homepage loads successfully without any HTTP or connection error.
    // expect: The page is fully accessible to an anonymous (not logged-in) user.
    // expect: No login, sign-up, or paywall barrier is displayed as a prerequisite to browsing.
    // expect: The search bar and language options are visible and enabled on the homepage.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Happy path - Search a valid topic and view the article content", async ({ page }) => {
    // step: Click the search bar on the Wikipedia homepage.
    // step: Type the topic 'Artificial intelligence'.
    // step: Press Enter or click the search (magnifier) button.
    // step: Wait for the result page to load.
    // step: If a search results list is shown, click the article titled 'Artificial intelligence'.
    // step: Read the article title, introduction paragraph, table of contents and section headings.
    // step: Scroll from the top to the bottom of the article.
    // expect: Search suggestions matching the typed topic are displayed while typing.
    // expect: The search is submitted successfully and the relevant article page loads without error.
    // expect: The page title matches the searched topic ('Artificial intelligence').
    // expect: Article content (headings, paragraphs, links, images, references) is rendered, readable and not truncated or overlapping.
    // expect: The article is viewable end to end without any login prompt or access restriction.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Negative - Submitting an empty search bar returns a blank page with no search results", async ({ page }) => {
    // step: Click into the search bar and leave it completely empty (enter no characters).
    // step: Press Enter or click the search button.
    // step: Observe the resulting page.
    // expect: No article page is opened.
    // expect: A blank search page is displayed with no search results listed.
    // expect: No application crash, stack trace, or unexpected error page is shown.
    // expect: The search bar remains available for a new search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Negative - Invalid / meaningless search text shows 'no results matching the query' message", async ({ page }) => {
    // step: Click the search bar.
    // step: Type the invalid text 'qwrtzplkxvbnmzzxq123'.
    // step: Press Enter or click the search button.
    // step: Wait for the search results page to load.
    // step: Read the message displayed on the results page.
    // expect: No article page is opened.
    // expect: The search results page displays the message: 'There were no results matching the query'.
    // expect: No results list is shown for the invalid text.
    // expect: The message is readable and the user can enter a new search term.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Negative - Search text longer than 500 characters shows maximum length error", async ({ page }) => {
    // step: Click the search bar.
    // step: Paste the 600-character test string into the search bar.
    // step: Press Enter or click the search button.
    // step: Wait for the response page to load.
    // step: Read the message displayed on the page.
    // expect: No article page is opened.
    // expect: The error message is displayed: 'An error has occurred while searching: Search request is longer than the maximum allowed length'.
    // expect: The page does not hang, crash, or display a raw server error.
    // expect: The user can clear the search bar and perform a new valid search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Boundary - Search text of exactly 500 characters vs exactly 501 characters", async ({ page }) => {
    // step: Click the search bar and paste the string of exactly 500 characters.
    // step: Confirm the character count in the search bar is 500.
    // step: Press Enter or click the search button and observe the result page.
    // step: Return to the search page and clear the search bar.
    // step: Paste the string of exactly 501 characters and confirm the count is 501.
    // step: Press Enter or click the search button and observe the result page.
    // expect: For the 500-character input, the maximum-length error is NOT displayed; the search is processed and 'There were no results matching the query' (or a valid results list) is shown.
    // expect: For the 501-character input, the error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: In both cases the page remains stable, readable, and the search bar stays usable.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
