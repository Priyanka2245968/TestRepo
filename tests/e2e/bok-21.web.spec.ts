import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Anonymous user searches for a valid topic and views the Wikipedia article (happy path)", async ({ page }) => {
    // step: Open the browser in private/incognito mode.
    // step: Navigate to https://www.wikipedia.org.
    // step: Confirm the homepage loads without prompting for login or registration.
    // step: Select English as the language.
    // step: Click into the search bar.
    // step: Type the topic 'Artificial intelligence'.
    // step: Press Enter (or click the search/magnifier button).
    // step: Scroll through the loaded article page from top to bottom.
    // expect: The Wikipedia homepage is fully accessible to an anonymous user with no login barrier.
    // expect: The search bar accepts the typed text and displays matching suggestions.
    // expect: After submitting, the browser navigates to the article page (URL contains /wiki/Artificial_intelligence) and the page loads successfully without errors.
    // expect: The article title 'Artificial intelligence' is displayed at the top of the page.
    // expect: Article body text, section headings, table of contents, and images render clearly and are readable without zooming.
    // expect: No error message, paywall, or access-restriction message is shown.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Search suggestions and navigation to article via suggestion dropdown", async ({ page }) => {
    // step: Click into the Wikipedia search bar.
    // step: Type 'Solar sys'.
    // step: Wait for the suggestion dropdown to appear.
    // step: Click the suggestion 'Solar System'.
    // expect: A suggestion dropdown appears listing article titles that match the typed prefix.
    // expect: 'Solar System' is present in the suggestion list.
    // expect: Clicking the suggestion loads the 'Solar System' article page successfully.
    // expect: The article content is visible and readable with no login prompt.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Negative - Submitting an empty search returns a blank page with no search results", async ({ page }) => {
    // step: Click into the search bar without typing any text.
    // step: Verify the search bar contains no characters (not even a space).
    // step: Press Enter (or click the search button).
    // expect: The search is submitted and the search results page is displayed.
    // expect: The page is blank with respect to results - no article results are listed.
    // expect: No article page is opened and no unexpected system/exception error is displayed.
    // expect: The application does not crash and the user remains on a usable Wikipedia page.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Negative - Search text longer than the maximum allowed length shows length error message", async ({ page }) => {
    // step: Click into the search bar.
    // step: Paste the prepared 600-character string into the search bar.
    // step: Press Enter (or click the search button).
    // step: Read the message displayed on the results page.
    // expect: The search results page loads.
    // expect: The error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: No article results are returned and no article page is opened.
    // expect: The page remains functional so the user can enter a new search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Negative - Invalid/nonsense search term shows 'no results matching the query' message", async ({ page }) => {
    // step: Click into the search bar.
    // step: Type the invalid term 'qwzxvptlmnbhgtrfd12345'.
    // step: Press Enter (or click the search button).
    // step: Read the message displayed on the results page.
    // expect: The search results page loads successfully.
    // expect: The message 'There were no results matching the query' is displayed.
    // expect: No article results are listed and no article page is opened.
    // expect: The search bar still contains the entered term, allowing the user to correct and re-search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Boundary - Search with exactly 500 characters vs 501 characters", async ({ page }) => {
    // step: Click into the search bar and clear any existing text.
    // step: Paste the 500-character string into the search bar.
    // step: Press Enter and observe the result page.
    // step: Return to the search bar and clear the text.
    // step: Paste the 501-character string into the search bar.
    // step: Press Enter and observe the result page.
    // expect: For the 500-character input, the search is processed without the length error; the page shows either matching results or the message 'There were no results matching the query'.
    // expect: For the 501-character input, the error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: No results are returned for the 501-character input.
    // expect: The application remains stable in both cases with no crash or unhandled exception.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-07 Article readability and accessibility on mobile viewport for anonymous user", async ({ page }) => {
    // step: Open the browser and set the viewport to a mobile device size (e.g., 375x667).
    // step: Navigate to https://www.wikipedia.org.
    // step: Search for 'Photosynthesis' using the search bar and submit.
    // step: Scroll through the article and expand a collapsed section.
    // expect: The homepage and article page load successfully without requiring login.
    // expect: The article page renders in a mobile-friendly layout with no horizontal scrolling required.
    // expect: Text is legible at default zoom and images scale within the screen width.
    // expect: Collapsed sections expand and display readable content.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
