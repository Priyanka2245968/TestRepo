import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 View Article on Wikipedia \u2014 happy path", async ({ page }) => {
    // step: Open the application base URL.
    // step: Authenticate as a valid user.
    // step: Navigate to the feature described by BOK-21.
    // step: Complete the primary action with valid input.
    // expect: The primary action succeeds without an error.
    // expect: A confirmation is shown to the user.
    // expect: The resulting state persists after a page reload.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 View Article on Wikipedia \u2014 rejects invalid input", async ({ page }) => {
    // step: Open the application base URL.
    // step: Authenticate as a valid user.
    // step: Navigate to the feature described by BOK-21.
    // step: Submit the form with a required field left empty.
    // expect: A validation message identifies the missing field.
    // expect: No record is created or modified.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 View Article on Wikipedia \u2014 unauthenticated access is denied", async ({ page }) => {
    // step: Request the BOK-21 feature URL directly without signing in.
    // expect: The user is redirected to the sign-in screen.
    // expect: No protected data is rendered.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 View Article on Wikipedia \u2014 boundary values are handled", async ({ page }) => {
    // step: Open the feature and locate the primary input.
    // step: Enter the maximum permitted length, then submit.
    // step: Enter one character beyond the maximum, then submit.
    // expect: The maximum permitted value is accepted.
    // expect: The over-length value is rejected with a clear message.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
