import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Anonymous user can access the Wikipedia homepage without login", async ({ page }) => {
    // step: Open a private/incognito browser window.
    // step: Navigate to https://www.wikipedia.org.
    // step: Observe the page load and the elements displayed.
    // step: Select the English language edition (en.wikipedia.org) if the language portal is shown.
    // expect: The Wikipedia homepage loads successfully with HTTP 200 and no error page.
    // expect: No login prompt, paywall, or access restriction is displayed.
    // expect: The search bar is visible and enabled on the homepage.
    // expect: The user remains anonymous (a 'Log in' / 'Create account' option is visible, no user name is shown).
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Happy path - Search a valid topic and view the article content", async ({ page }) => {
    // step: Click into the search bar on the homepage.
    // step: Type the valid topic 'Artificial intelligence'.
    // step: Press Enter (or click the search/magnifier button) to submit the search.
    // step: Wait for the result page to load.
    // step: Read the article title, lead paragraph, table of contents and section headings.
    // step: Scroll down through the full article to the References section.
    // expect: Search suggestions appear in a dropdown while typing.
    // expect: The relevant article page 'Artificial intelligence' loads successfully without any error.
    // expect: The article title is displayed as a heading matching the searched topic.
    // expect: Article content (lead paragraph, sections, images, table of contents, references) is fully rendered, readable and not truncated or blocked.
    // expect: No login prompt or restricted-access message is shown at any point.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Negative - Submitting an empty search returns a blank search results page", async ({ page }) => {
    // step: Click into the search bar without typing any text.
    // step: Press Enter (or click the search/magnifier button) to submit the empty search.
    // step: Observe the resulting page.
    // expect: The search results page opens with a blank result area and no search results listed.
    // expect: No article page is opened and no unexpected redirect occurs.
    // expect: No application crash, stack trace, or system error message is displayed.
    // expect: The search bar remains available so the user can enter a new query.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Negative - Invalid / meaningless search text shows 'no results matching the query' message", async ({ page }) => {
    // step: Click into the search bar.
    // step: Type the invalid text 'zxqwvytplkjhgfdsa1234'.
    // step: Press Enter to submit the search.
    // step: Observe the message displayed on the search results page.
    // expect: The search results page loads successfully.
    // expect: The message 'There were no results matching the query' is displayed.
    // expect: No article is opened and no result entries are listed.
    // expect: No system/HTTP error page is shown.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Negative - Search text longer than the maximum allowed length shows length error", async ({ page }) => {
    // step: Click into the search bar.
    // step: Paste the prepared 600-character string into the search bar.
    // step: Press Enter to submit the search.
    // step: Observe the message displayed on the search results page.
    // expect: The search results page loads without crashing or timing out.
    // expect: The error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: No search results are returned and no article page is opened.
    // expect: The page layout is not broken by the long input string.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Boundary - Search text of exactly 500 characters vs 501 characters", async ({ page }) => {
    // step: Click into the search bar and paste the 500-character string.
    // step: Press Enter to submit the search and observe the result page.
    // step: Return to the search bar and clear the previous input.
    // step: Paste the 501-character string.
    // step: Press Enter to submit the search and observe the result page.
    // expect: For the 500-character query: the length error message is NOT displayed; the search is processed and the page shows either matching results or 'There were no results matching the query'.
    // expect: For the 501-character query: the error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: In both cases the application does not crash and the page remains usable for a new search.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
