import { test, expect } from "@playwright/test";

// Generated by One Touch E2E for BOK-21.
// Steps and expectations from the approved manual cases are preserved as
// comments so a human can flesh out selectors without losing intent.
const BASE_URL = process.env.BASE_URL ?? "http://localhost:3000";

test.describe("ViewArticleOnWikipedia (web)", () => {
  test("TC-01 Anonymous user can search for a topic and view the Wikipedia article (happy path)", async ({ page }) => {
    // step: Open a private/incognito browser window.
    // step: Navigate to https://www.wikipedia.org.
    // step: Select English as the language and confirm the homepage loads without any login prompt.
    // step: Click the search bar on the homepage.
    // step: Type the topic 'Artificial intelligence'.
    // step: Press Enter (or click the search/magnifier button).
    // step: Observe the loaded page and scroll through the article content.
    // expect: Wikipedia homepage loads successfully for the anonymous user with no login or paywall barrier.
    // expect: The search bar accepts the typed text and shows suggestions.
    // expect: After submitting, the 'Artificial intelligence' article page loads successfully with HTTP 200 and no error message.
    // expect: The article title matches the searched topic.
    // expect: Article content (introduction, sections, table of contents, images/links) is fully visible, readable, and scrollable without requiring login.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-02 Article can be opened by selecting a suggestion from the search bar dropdown", async ({ page }) => {
    // step: Navigate to https://en.wikipedia.org.
    // step: Click the search bar.
    // step: Type 'Solar sys' without pressing Enter.
    // step: Wait for the auto-suggestion dropdown to appear.
    // step: Click the suggestion 'Solar System'.
    // expect: Auto-suggestion dropdown displays a list of matching article titles including 'Solar System'.
    // expect: Clicking the suggestion navigates directly to the 'Solar System' article page.
    // expect: The article loads successfully and its content is readable without login.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-03 Negative - Submitting an empty search bar returns a blank result page", async ({ page }) => {
    // step: Navigate to https://www.wikipedia.org (English).
    // step: Click the search bar and leave it empty (do not type any character).
    // step: Press Enter (or click the search button).
    // step: Observe the resulting page.
    // expect: No article page is opened.
    // expect: A blank search page is displayed with no search results listed.
    // expect: No system exception, crash, or unhandled error is shown to the user.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-04 Negative - Invalid / non-existent search text shows 'no results matching the query' message", async ({ page }) => {
    // step: Navigate to https://en.wikipedia.org.
    // step: Click the search bar.
    // step: Type the invalid string 'qwzxvbnmlkjhgf12345'.
    // step: Press Enter to submit the search.
    // step: Observe the search results page.
    // expect: The search results page loads without crashing.
    // expect: The message 'There were no results matching the query' is displayed.
    // expect: No article results are listed for the invalid text.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-05 Negative - Search text longer than the maximum allowed length shows length error message", async ({ page }) => {
    // step: Navigate to https://en.wikipedia.org.
    // step: Click the search bar.
    // step: Paste the prepared string of more than 500 characters into the search bar.
    // step: Press Enter to submit the search.
    // step: Observe the resulting page.
    // expect: No article page is opened.
    // expect: The error message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: The page remains usable and the search bar still allows a new search to be entered.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
  test("TC-06 Boundary - Search with exactly 500 characters vs 501 characters", async ({ page }) => {
    // step: Navigate to https://en.wikipedia.org.
    // step: Paste the 500-character string into the search bar and press Enter.
    // step: Record the page/message that is displayed.
    // step: Return to the Wikipedia homepage.
    // step: Paste the 501-character string into the search bar and press Enter.
    // step: Record the page/message that is displayed.
    // expect: For the 500-character input the maximum-length error is NOT shown; the search is processed and the results page displays 'There were no results matching the query' (or matching results if any exist).
    // expect: For the 501-character input the message 'An error has occurred while searching: Search request is longer than the maximum allowed length' is displayed.
    // expect: No crash, blank screen, or unhandled exception occurs in either case.
    await page.goto(BASE_URL);
    await expect(page).toHaveURL(/.*/);
    await expect(page.locator("body")).toBeVisible();
  });
});
